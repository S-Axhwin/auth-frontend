import React from 'react';
import Form from './Form';

const Wrapper = () => {
  return (
    <div style={{width: '40vw', margin: 'auto', backgroundColor: 'gray', padding: '2rem 1rem', marginTop: '4rem',borderRadius: '1rem'}}>
    <Form></Form>
    </div>
  )
}

export default Wrapper